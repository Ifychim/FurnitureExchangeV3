import React  from "react";

//importing styles
import useStyles from "./styles";

const Form = () => {
    const classes = useStyles();
    return (
        <h1>FORM</h1>
    );
}

export default Form;